You will need following libs in order to use libmetamon:

- SDL2, SDL2_ttf, SDL2_mixer, SDL2_image

Compile only:
	make && make lib

To install:
	make all - or - make && make lib && make install

Enjoy
